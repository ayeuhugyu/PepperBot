for update:

to test:

shit on the todo list:

[1] - add p/corrupt image support
[2] - ai image processing
[3] - fix p/queue resetting the page when refreshing
[4] - add a p/embed setfootericon, setauthoricon, addfield, removefield, seturl
[5] - remake p/reactionrole, the syntax is ust horrific

p/update 

**actual content:**

- updated website since last time i said anything (logs)
- pepperbot may or may not have status now? idk it won't show up for me
- p/eval command (lmao you thought i wouldn't do it (i see actual use for me for it now))
- p/restart can now restart the host process (meaning i can make changes to index.js and then actually use said changes)

**fixes/optimizations:**

- fixed p/embed: subcommand missing choices for save / load
- fixed errors caused by undefined arguments
- im using nodemon instead of node now, basically it restarts itself amd probably some other stuff idk